using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DestructionVfxTrajectoire : MonoBehaviour {

    public GameObject VfxTrajectoireTir;
    // Use this for initialization
    void Start () {
		
	}
	/*public void ParentCanon(GameObject Canon)
    {
        VfxTrajectoireTir.transform.parent = Canon.transform;
        Debug.Log("Player's Parent: " + VfxTrajectoireTir.transform.parent.name);
    }
    */
	// Update is called once per frame
	void Update () {
		
	}

    /*private void OnTriggerEnter(Collider other)
    {
        Destroy(gameObject);
    }
*/}
